### A simple get request to the test endpoint. No need for login
GET {{url}}/auth/test/


### Create a new user - this one will only get a USER role by default
POST {{url}}/auth/register/

{
    "username": "user223",
    "password": "test123"
}

### Login a user and get a Token in response that is saved in a global variable
POST {{url}}/auth/login/

{
    "username": "admin",
    "password": "admin123"
}

> {%
    client.global.set("jwt_token", response.body.token);
    console.log("JWT Token:", client.global.get("jwt_token"));
%}




### Add a quote to users favorites
POST http://localhost:7076/api/v1/users/admin/favorites/3
Authorization: Bearer {{jwt_token}}

### Add a quote to users favorites
DELETE http://localhost:7076/api/v1/users/admin/favorites/3
Authorization: Bearer {{jwt_token}}
